import React from 'react';
import { Body, Title, Option, Arrow } from './style';

const Menu = () => {
    return (
        <Body>
            <Title>Seja bem vindo(a)</Title>
            <Option><Arrow />Cursos</Option>
        </Body>
    );
};

export default Menu;